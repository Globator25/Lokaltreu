name: gdpr-compliance
on: [pull_request]
jobs:
  gdpr-compliance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node .github/scripts/gdpr-checks.mjs
        # Checks: Art.11 DSR-Flows vorhanden, Retention=180T in Jobs/Policies, keine PII in Logs.
