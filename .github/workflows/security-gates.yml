name: security-gates

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  security:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Security gates placeholder
        run: |
          echo "Security gates (Anti-Replay, Device-Proof, Plan-Gate) werden in einem späteren Roadmap-Schritt mit echter API aktiviert."
