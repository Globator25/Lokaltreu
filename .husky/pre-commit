#!/usr/bin/env sh
set -e

if command -v pwsh >/dev/null 2>&1; then PWSH="pwsh"; else PWSH="powershell"; fi
ROOT="$(git rev-parse --show-toplevel)"
"$PWSH" -NoProfile -ExecutionPolicy Bypass -File "$ROOT/scripts/agents-sentinel-check.ps1"
