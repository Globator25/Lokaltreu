// apps/web/scripts/ci-web-test-placeholder.cjs
// Placeholder für Frontend-Tests – wird in Schritt 27/37 ersetzt.

console.log('TODO: Web-Tests folgen ab Roadmap-Schritt 27/37');

// Alle übergebenen Argumente (z. B. --coverage) einfach ignorieren:
if (process.argv.length > 2) {
  console.log('Args (ignoriert):', process.argv.slice(2));
}

// Erfolgreich beenden
process.exit(0);
