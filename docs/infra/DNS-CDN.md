# DNS & CDN (EU Regional Services)
api.<tenant>.lokaltreu.*  → Origin: Fly.io (eu-central), API /v1
app.lokaltreu.*           → PWA via Cloudflare CDN, Regional Services: EU aktiv
Hinweis: TLS-Terminierung in EU, kein Edge-Processing außerhalb EU.
