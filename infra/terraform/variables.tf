variable "env" { type = string }        # dev | stage | prod
variable "region_api" { type = string } # z.B. eu-central
variable "region_cdn" { type = string } # z.B. eu (Cloudflare Regional Services)
